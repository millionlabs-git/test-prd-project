{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A simple vanilla JS todo list app with localStorage persistence",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint js/ tests/",
    "serve": "npx serve . -l 3000"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "eslint": "^9.20.0",
    "globals": "^15.14.0",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
