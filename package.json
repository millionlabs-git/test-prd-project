{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Vanilla JS Todo App — no build step, Tailwind CDN, localStorage persistence.",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "serve . --listen 3000 --no-clipboard",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
