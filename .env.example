# ─── Environment Variables ────────────────────────────────────────────────────
#
# This is a fully client-side, static application with no backend server,
# no API keys, and no external services. There are no required environment
# variables at this time.
#
# If you add a backend or cloud-sync feature in the future, add the necessary
# variables here and commit this file (never commit .env with real secrets).
#
# Example (for future use):
#   API_BASE_URL=https://api.example.com
#   API_KEY=your-api-key-here
