# ─────────────────────────────────────────────────────────────
# .env.example — Todo App
#
# This is a purely client-side, static application.
# There are NO server-side environment variables required to run
# the production app (index.html + app.js + styles.css).
#
# The variables below are used only for the development/test
# toolchain (Playwright E2E tests, local file server).
# ─────────────────────────────────────────────────────────────

# Port used by the `serve` static file server during development and E2E tests.
# Matches the baseURL in playwright.config.js.
# Default: 3000
PORT=3000

# Set to "true" in CI environments.
# Affects Playwright: disables reuse of an already-running server, enables
# stricter parallelism limits, and activates GitHub Actions reporter.
# Default: (unset / false)
CI=false
